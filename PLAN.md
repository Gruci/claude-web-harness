# PLAN.md — 하네스 커널 분리

> 담는 것: fund_monitor 전용 하네스를 커널+프로파일 구조로 가르는 작업의 단계·검증 기준·결정 기록. 담지 않는 것: 하네스 작동 원리(→ `index.html` 이하 5쪽)·현재 실물의 사양(→ `src/`). 읽는 시점: 이 리팩터를 이어서 할 때.

## 목표

이 하네스를 들고 **새 사이트를 처음부터 만들 수 있어야 한다.** 이식이 목표가 아니라, 새 프로젝트에서 첫날부터 가동되는 게 목표다. 새 사이트는 DB도 프론트도 자기 구조를 갖고, 그때 fund_monitor 의 규칙이 따라오면 안 된다.

## 왜 지금 안 되나 — 실측 (2026-08-05)

게이트 모듈 14개에 프로젝트 고유 토큰 **50개**가 박혀 있다. `static_check_tests.py` 는 도메인 패키지 12개를 나열하고 `static_check_md.py` 는 `DEVGUIDE` 를 12번 참조한다. 새 프로젝트에 얹으면:

| | 게이트 | 결과 |
|---|--------|------|
| 즉시 차단 | ④A · ④E | plan MD 의 미래 경로 참조에서 FAIL. 첫 문서도 못 쓴다 |
| 조용히 무음 | ④B ④C ⑧ ⑨ ⑫ ⑮ ⑰~㉕ | 대상 파일 0개인데 `[OK]` 로 찍힌다 |
| 실제 동작 | 9개 | 나머지 |

**차단보다 무음이 위험하다.** `_print_sections()` 가 "위반 0건"과 "대상 0개"를 구분하지 않아, 지켜준다고 믿는 게 안 지켜진다.

## 구조 — 커널은 규칙을 갖지 않고 규칙의 형태만 갖는다

```
kernel/          프로젝트 무관. 판정 로직과 훅
profiles/        프로젝트별 이름·어휘·대조 쌍. 새 프로젝트가 채우는 유일한 곳
gates_local/     프로젝트 전용 게이트. 사고가 나면 하나씩 는다
tests/           픽스처 + 골든 출력
```

커널은 "읽기 레이어는 쓰기 SQL 을 하면 안 된다"는 판정기를 갖고, 그 레이어가 어디인지는 프로파일이 정한다. 선언이 없으면 `[SKIP]`.

### 게이트 25개 재분류

| 분류 | 개수 | 내역 |
|------|------|------|
| 커널 그대로 | 9 | 1 · 2 · ⑩⑪ · ㉒ · ⑬a~g · ④A ④D ④E |
| 커널 + 어휘 데이터 | 3 | 4 · 4b · 5 |
| 커널 + 레이어 이름 | 8 | 3 · ①②③⑤⑥⑦ · ⑧⑨ · ⑫ · ⑭ · ⑮ |
| `DOC_SYNC` 규칙으로 흡수 | 2 | ④B · ④C |
| `gates_local/` 플러그인 | 9 | ⑯⑰⑱⑲⑳㉑㉓㉔㉕ |

프로젝트 전용은 9개뿐이다. 나머지 16개는 로직을 안 고치고 프로파일 한 파일로 살아난다.

## 마찰 제거 장치 3개

1. **`[SKIP]` 등급** — 대상 0개면 `[OK]` 대신 `[SKIP] ⑮API 배열 옵셔널 — ui 레이어 미선언`. 무음 무력화가 눈에 보인다. 이 리팩터에서 값이 가장 크다.
2. **`--adopt`** — 현재 위반 전량을 baseline 으로 동결하고 게이트를 켠다. 게이트 도입 마찰이 0이 되고 래칫(감소만 허용)은 유지된다.
3. **`STAGE="greenfield"`** — MD 가 코드보다 먼저 나오는 시기엔 ④A 를 `[REPORT]` 로 강등하고, 파일이 실제로 생기면 승격 가능 리포트를 낸다.

## 단계와 검증 기준

| | 작업 | 통과 기준 |
|---|------|-----------|
| P0 | 레포 승격 · `profiles/_template.py` 계약 · `profiles/fund_monitor.py` | 완료 |
| P1 | `tests/fixtures/miniproj/` + 골든 출력 확보 | 완료 — 35개 섹션 중 27개 발화, 3회 연속 동일 |
| P2 | `kernel/gates/` 패키지 분리 + `[SKIP]` 등급 | 완료 — 이동 단계는 바이트 동일, `[SKIP]` 단계는 3줄만 변경 |
| P3 | 프로젝트 전용 게이트 9개 삭제 + 하드코딩 138개를 프로파일로 | 완료 — 커널 잔재 0, 게이트 25개 전량 발화 |
| P4 | `--adopt` 자동 baseline + `/harness-init` 스킬 | 빈 디렉토리에서 한 번에 가동 |
| P5 | 훅 7종 이관 + `CLAUDE.md`(골격) ↔ `PROJECT.md`(도메인) 분리 | 새 폴더에서 세션 시작이 정상 동작 |
| P6 | PreToolUse(Bash) 훅 · 지도 대조 양방향 · 문서 5쪽 재작성 | 각 항목 개별 검증 |

**P3 에서 방향을 바꿨다.** 당초 계획은 프로젝트 전용 게이트를 `kernel/local/` 에 두고 프로파일로 켜고 끄는 것이었으나, 그러면 커널이 남의 프로젝트 규칙을 계속 이고 간다. KRX·KOFIA·로스터·프롬프트 버전은 **삭제**했다. 새 프로젝트는 자기 레포의 `harness_gates/` 에 자기 규칙을 심는다.

P2·P3 의 **골든 diff 0** 이 안전망이다. 리팩터 중 게이트가 조용히 약해지는 것을 그 기준 하나로 막는다. P2 가 끝나면 399줄 문제도 같이 사라진다.

## 결정 기록

| 결정 | 선택 | 근거 |
|------|------|------|
| 작업 위치 | `harness/` 독립 레포 승격 | 새 사이트마다 DB·프론트 규칙이 다르다. 레포 안 리팩터는 fund_monitor 구조를 계속 끌고 간다 |
| 프로파일 형식 | `profile.py` dict | 의존성 0, 주석 가능, 조건 분기 가능. `--adopt` 자동 생성도 걸림돌이 없다 |
| 게이트 기본 상태 | 전부 켜짐 + `[SKIP]` + `--adopt` | opt-in 은 "등재를 잊으면 조용히 안 돈다"는 지금 문제를 형태만 바꿔 남긴다 |

## 알려진 부채

- 독립 레포로 가면 "fund_monitor 에서 돌려 출력 비교"라는 공짜 회귀 기준이 끊긴다. P1 의 픽스처·골든이 그 대체이고, 최종 확인은 레포에서 1회 수동으로 한다.
- 커널 게이트 25개는 전량 골든이 덮는다(각 1건씩 발화). 죽은 게이트가 없다.
- **fund_monitor 는 게이트 9개를 잃은 상태다.** 커널에서 삭제했고 아직 그 레포에 심지 않았다. 원본은 `src/static_check_{krx,llm,region,dup,complete_date,batches,prompt}.py` 이고, `harness_gates/<이름>.py` 로 옮기며 `run(py, ui)` 계약에 맞춰야 한다. `profiles/fund_monitor.py` 의 `LOCAL_GATES` 가 그 목록이다.
- `ui_hex` 예외 66개는 아직 프로파일에 안 옮겼다. 목록이 길어 레포 쪽 `hex_allowlist.txt` 로 뺄지 프로파일에 넣을지 이관 시점에 정한다.
- 훅 7종은 아직 `src/.claude/hooks/` 에만 있다. P5 에서 커널로 옮긴다.
- ⑯ 은 무음이 아니라 **틀리게 발화**한다 — `market_briefing/prompts/` 를 하드코딩해 그 파일이 없는 프로젝트에서 "대상 프롬프트 파일 없음"으로 실패한다. 새 프로젝트를 처음부터 막는 두 번째 게이트다(④E 와 함께).
- `src/` 는 리팩터 전 동결 스냅샷이다. P3 이 끝나면 지운다.
- baseline·allowlist 실물 4종이 아직 없다. `src/README.md` 의 복사 명령을 레포 루트에서 한 번 돌려야 채워진다.
