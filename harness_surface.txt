# harness_surface.txt — 편집 표면 동결 목록. 게이트가 봐주기로 한 것의 전량이다.
#
# 형식: <표면 키>\t<값>
# 판정: harness_gates/edit_surface.py — 프로파일 선언이 이 목록보다 늘면 위반이다.
#
# 래칫: 이 파일은 줄어들기만 해야 한다. 면제를 없앴으면 해당 행을 지운다.
#       늘려야 한다면 그건 게이트를 무르게 만드는 변경이다. 코드를 고치는 쪽이 먼저이고,
#       그래도 필요하면 사유를 dev/REJECTED.md 에 남긴 뒤 같은 커밋에서 여기에 더한다.
#
# 새 프로젝트에 하네스를 깔면 이 파일도 같이 딸려온다. 그때는 그 프로젝트의 실제 표면으로
# 다시 떠라 — `python -X utf8 -c "from harness_gates.edit_surface import current
# ; [print(f'{k}\t{v}') for k, v in sorted(current())]"` 가 현재 선언을 그대로 뽑는다.
SCOPE["exclude_all"]	tests/fixtures/
