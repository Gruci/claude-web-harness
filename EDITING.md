# EDITING.md — 과업 보드 & 백로그

> 담는 것: 양쪽 에이전트의 작업 소유권과 격리 규칙. 담지 않는 것: 개발 절차(→ `dev/workflows/feature-workflow.md`)·훅 설정(→ `HARNESS.md`). 읽는 시점: 모든 편집 직전.

## 작업 소유권

- 메인 세션이 작업 범위와 세션 식별자를 아래 보드에 등록한다.
- 실행 환경의 session_id를 알면 앞 8자를 `#sid`로 쓴다.
- 식별자가 제공되지 않으면 작업용 ID를 한 번 생성해 사용하고 런타임 ID로 검증됐다고 주장하지 않는다.
- 자기 행만 갱신하며 다른 세션의 작업과 미커밋 변경을 보존한다.
- 같은 API 계약이나 화면을 동시에 변경할 때는 순서를 조율한다.
- 위임 워커는 부모의 작업 행을 공유하며 배정된 파일만 수정한다.

## 작업 격리

작업 전 `git worktree list`와 현재 브랜치를 확인한다.
추가 worktree가 없고 단일 세션이면 메인 체크아웃에서 작업할 수 있다.
추가 worktree가 있으면 공유 메인 체크아웃은 조회용이며 구현은 자기 worktree에서 한다.

| 실행 환경 | 생성 방법 | 식별 규칙 |
|---|---|---|
| Claude Code | 지원되는 worktree 도구 또는 git 명령 | 도구에 넘기는 이름에도 작업 ID 포함 |
| Codex | `git worktree add` | 현재 체크아웃과 브랜치를 실행 전후 확인 |
| 양쪽 공통 | 기존 `.claude/worktrees/<주제>--<sid8>` 위치 사용 | 보드와 같은 `--<sid8>` 접미 |

경로 이름은 기존 훅과 설치본의 호환 계약이며 Claude 지침을 읽으라는 뜻이 아니다.
외부 worktree에서도 해당 체크아웃의 커널을 사용한다.
내장 worktree 디렉터리는 로컬 git 제외 설정에 등록해 검색과 검사 대상에 섞이지 않게 한다.

- 생성 기준 브랜치는 원격 기본 브랜치의 실물을 확인해 고른다.
- 브랜치 이름은 작업 성격에 맞는 접두를 사용한다.
- 구현과 검증은 같은 worktree에서 수행한다.
- 의존성 디렉터리를 다른 worktree에 junction이나 symlink로 연결하지 않는다.
- 필요한 의존성은 해당 트리에 설치한다.
- 같은 기능을 위임한 워커는 부모 worktree를 공유하며 편집 소유권으로 충돌을 방지한다.
- 별도 PR을 만드는 독립 트랙만 별도 worktree로 격리한다.
- 실행 중인 워커의 worktree는 제거하지 않는다.
- 자동 정리되는 실행 환경에서 독립 트랙을 쓰면 작업 전에 미추적 표식과 git worktree lock으로 보호한다.

## 완료와 정리

1. 승인된 범위의 구현과 검증을 완료한다.
2. 두 작업 문서를 공통 개발 절차에 따라 보관한다.
3. 커밋과 원격 반영은 사용자 승인 범위와 실행 환경 지침에 맞춰 수행한다.
4. 머지한 자기 worktree만 제거하고 그다음 자기 브랜치를 정리한다.
5. 자기 작업 행을 마지막에 제거한다.

보드 행이 있다는 이유만으로 진행 중인 세션을 종료 불능으로 만들지 않는다.
머지 여부를 추론한 잔여 작업 판정은 경고이며 확인 없이 파일이나 행을 삭제하지 않는다.
현재 훅의 차단과 경고 구분은 `HARNESS.md`가 설명한다.

## 🔒 과업 보드 (Active Edits)

> 파일 나열 대신 **과업 스코프 서술**을 적는다. 어느 화면, 어느 API, 어느 영역인지.
> 진행 상태와 정리 책임은 자기 `#sid` 행으로 식별한다.

| 과업(브랜치) #sid | 스코프 요약 | 시작 | 상태 |
|---|---|---|---|
<!-- 예시:
| feat/peers-progressive #sid:a1b2c3d4 | peers AUM 탭 점진 로딩 — API 분할 + 소비 훅 개편 | 2026-07-28 | 진행 |
-->

---

## 📋 잔여 백로그

> **열린 것만 남긴다.** 완료·기각 행은 지운다 — 경위는 git 이력과 `docs/tasks/archive/`에 있고, 여기 쌓이면 "지금 뭐가 남았나"를 못 읽는다.
> ⚠️ **착수 전에 서술을 실물로 대조한다.** 원류 정리에서 8건 중 5건이 "착수해보니 이미 해결돼 있었다"였고 3건은 전제가 통째로 낡아 있었다. 백로그는 **관측이 아니라 기억**이다.

| 우선순위 | 항목 | 설명 |
|----------|------|------|
| 🟡 중간 | P6 잔여 — PreToolUse(Bash) 훅·지도 대조 양방향 | 커널 분리 plan의 미착수분. 지도에만 남은 유령 항목은 현재 안 잡힌다 |
| 🟡 중간 | fund_monitor 게이트 9개 복원 | 커널에서 삭제됐고 그 레포에 아직 안 심음. 원본은 커밋 `ee9805c`, `harness_gates/` 로 이식하며 `run(py, ui)` 계약에 맞춘다 |
| 🔵 낮음 | ui_hex 예외 66개 이관 | fund_monitor 프로파일로 옮기며 별도 파일 vs 프로파일 인라인 결정 |
| 🔵 낮음 | impeccable 벤더 2.4MB | 웹 UI 없는 프로젝트엔 순수 무게. 프리셋별 제외 여부 미결정 |
| 🔵 낮음 | typecheck·pytest 실행 게이트 | `frontend/`와 `tests/`가 생기면 Stop 훅에 타입체크와 pytest 실행을 추가 |
| 🔵 낮음 | 첫 수직 슬라이스 | 테이블 1개 + 라우트 1개 + 화면 1개. 게이트 20~27이 실코드에 처음 부딪히는 지점 — 오탐은 baseline이 아니라 게이트 수정으로 대응 |
| 🔵 낮음 | API 계약 자동 검증 | 라우트 응답 스키마와 프론트 타입 대조를 `qa` 에이전트 수동에서 스크립트 게이트로 승격 검토 |
| 🔵 낮음 | security-reviewer 에이전트 | 인증·결제 도메인 진입 시 추가 |
| 🔵 낮음 | 중첩 def allowlist 메커니즘 | 데코레이터 등 정당한 클로저가 필요해지는 시점에 구현 (현재 전면 금지) |
