# EDITING.md — 과업 보드 & 백로그

> Claude Code 세션이 착수 전 반드시 확인하는 파일.
> 과업(의미) 충돌 방지 + 미완료 작업 추적 용도. 동적 파일이라 import 금지 — 항상 fresh Read.

---

## 🚦 worktree 병렬 프로토콜

**단일 워킹트리에서 병렬 세션을 굴리면 셋이 깨진다.** `git add -A`가 남의 작업을 쓸어담고, 브랜치 전환이 남의 미커밋 WIP를 부수고, 잠금이 데드락을 만든다. 해법은 잠금 세분화가 아니라 **격리**다 — 세션마다 자기 worktree, 파일 충돌은 git 머지가 줄 단위로 판정한다.

### 세션이 하나뿐일 때 (신규 프로젝트 기본)

과업 보드에 행만 등록하고 메인 체크아웃에서 그대로 작업한다. 커밋 후 바로 푸시해서 "로컬에만 있는 커밋" 상태를 최소화한다.

### 병렬 체제 — worktree 가 하나라도 생기는 순간부터

worktree 가 존재하면 **공유 메인 체크아웃은 자동으로 구현 금지 구역**이 된다(리서치 Read·서버 실행·조회 전용). `check_bash_write.py` 가 공유 트리의 git 변경 명령을 차단한다 — 체제 전환은 선언이 아니라 관측으로 일어난다.

1. **착수** — 아래 보드를 확인한다. 같은 화면·같은 API 계약·동시 편집 금지 파일을 건드리는 진행 중 과업이 있으면 조율 후 순차로 시작한다. 보드에 행을 등록하되 **`#sid` 태그가 필수**다. 자기 scratchpad 경로 UUID의 앞 8자를 쓴다.
2. **격리** — `EnterWorktree` 툴을 쓴다. 자리는 **레포 안 `.claude/worktrees/<주제>--<sid8>`** 다. 접미가 보드 행의 `#sid`와 같아서 `git worktree list` 하나로 "누가 무엇을 잡고 있나"가 조인된다 — 없으면 추측하게 된다. `check_worktree_name.py`가 생성 시점에 강제한다. 수동 생성도 같은 자리: `git worktree add .claude/worktrees/<주제>--<sid8> -b <브랜치> origin/<기본 브랜치>`. 브랜치 네이밍은 `feat/` `fix/` `perf/` `chore/` `docs/`. `.claude/worktrees/`는 `.git/info/exclude`에 등재한다 — 레포 안이어도 게이트(`git ls-files` 수집)와 검색에 안 섞인다. info/exclude는 클론에 전파되지 않으므로 온보딩(`/harness-init`)이 등재한다.
2-1. **격리 밖으로 링크를 잇지 않는다.** 의존성을 다시 깔기 싫어 worktree의 `node_modules`를 공유 트리로 junction 거는 것이 대표 경로다. 한쪽을 걷을 때 삭제가 그 실을 타고 들어가 **원본이 비워진다.** 의존성은 그 트리에서 직접 깔고, 파일이 필요하면 링크 말고 복사한다. `check_bash_write.py`가 생성 시점에 막는다.
3. **구현·검증**은 전부 worktree 안에서 한다. 게이트와 테스트와 빌드가 동일하게 적용된다.
3-1. **병렬 위임 워커는 격리하지 않는다** — 비격리 서브에이전트는 부모의 worktree를 cwd로 상속한다. `isolation:"worktree"`를 주면 워커마다 브랜치가 갈라져 브랜치 1개 계약이 깨진다. 한 worktree 공유라 git이 파일 충돌을 못 잡는다 — 소유권 배정이 유일한 방어이고 정본은 `orchestrator` 에이전트다. 위임 직전 베이스라인 커밋을 만든다 — 워커 부분 실패 시 선별 롤백의 기준점이다.
4. **완료** — 원격에 PR·CI가 있으면 push → PR 생성 → CI 통과 확인 → merge, 없으면 기본 브랜치에 merge 후 push. 머지 충돌 시 자기 브랜치에 기본 브랜치를 merge해 해소 후 재시도한다(공유 트리를 건드리지 않는다). 끝나면 보드 행을 제거한다.
5. **잔가지 제거 — 머지 직후 같은 턴에** — `git worktree remove` → `git branch -d <브랜치>` 순서다. 순서가 계약이다 — worktree가 점유 중인 브랜치는 로컬 삭제가 거부된다. 미루면 죽은 worktree가 쌓여 `git worktree list`의 조인이 무의미해진다. Stop 훅 `check_worktree_residue.py`가 잔존 시 종료를 막는다.

**`#sid` 태그의 역할** — Stop 훅이 **자기 행만** 보고 종료를 차단한다. 태그가 없으면 훅이 세션을 식별하지 못해 전 행을 차단하고, 그러면 남의 잠금 때문에 내 세션이 못 끝난다.

## 🔒 과업 보드 (Active Edits)

> 파일 나열 대신 **과업 스코프 서술**을 적는다. 어느 화면, 어느 API, 어느 영역인지.
> Stop 훅이 자기 `#sid` 행 잔존 시 종료를 차단한다 = 완료 보고 강제.

| 과업(브랜치) #sid | 스코프 요약 | 시작 | 상태 |
|---|---|---|---|
<!-- 예시:
| feat/peers-progressive #sid:a1b2c3d4 | peers AUM 탭 점진 로딩 — API 분할 + 소비 훅 개편 | 2026-07-28 | 진행 |
-->

---

## 📋 잔여 백로그

| 우선순위 | 항목 | 설명 |
|----------|------|------|
| 🟡 중간 | P6 잔여 — PreToolUse(Bash) 훅·지도 대조 양방향 | 커널 분리 plan의 미착수분. 지도에만 남은 유령 항목은 현재 안 잡힌다 |
| 🟡 중간 | fund_monitor 게이트 9개 복원 | 커널에서 삭제됐고 그 레포에 아직 안 심음. 원본은 커밋 `ee9805c`, `harness_gates/` 로 이식하며 `run(py, ui)` 계약에 맞춘다 |
| 🔵 낮음 | ui_hex 예외 66개 이관 | fund_monitor 프로파일로 옮기며 별도 파일 vs 프로파일 인라인 결정 |
| 🔵 낮음 | impeccable 벤더 2.4MB | 웹 UI 없는 프로젝트엔 순수 무게. 프리셋별 제외 여부 미결정 |
| 🔵 낮음 | typecheck·pytest 실행 게이트 | `frontend/`와 `tests/`가 생기면 Stop 훅에 타입체크와 pytest 실행을 추가 |
| 🔵 낮음 | 첫 수직 슬라이스 | 테이블 1개 + 라우트 1개 + 화면 1개. 게이트 20~27이 실코드에 처음 부딪히는 지점 — 오탐은 baseline이 아니라 게이트 수정으로 대응 |
| 🔵 낮음 | API 계약 자동 검증 | 라우트 응답 스키마와 프론트 타입 대조를 `qa` 에이전트 수동에서 스크립트 게이트로 승격 검토 |
| 🔵 낮음 | security-reviewer 에이전트 | 인증·결제 도메인 진입 시 추가 |
| 🔵 낮음 | 중첩 def allowlist 메커니즘 | 데코레이터 등 정당한 클로저가 필요해지는 시점에 구현 (현재 전면 금지) |
