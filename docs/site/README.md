# docs/site — 병합 전 설명서 (STALE)

> 담는 것: 이 폴더가 왜 낡았는지. 담지 않는 것: 현재 하네스의 사양(→ 레포 루트 `README.md`·`HARNESS.md`). 읽는 시점: 이 폴더의 HTML 을 열어보기 전에.

이 5쪽은 커널 병합 **이전**의 검사기를 설명한다. 그때는 검사기가 루트에 평면 배치된
`static_check*.py` 였고 설정은 `harness_config.py` 였다.

지금 실물은 `kernel/` 패키지이고 프로젝트 설정은 `harness_profile.py` 다. 게이트 번호도
다시 매겨졌다. 그래서 이 폴더의 내용은 현재 실물과 어긋난다.

현재의 정본은 레포 루트의 `README.md` 와 `HARNESS.md` 다. 다시 쓰기 전까지 여기를 근거로
삼지 않는다.
